
function dev(name, jobTitle, company, experience, school, major, email, linkedInUrl, codeLanguages) {
    let name = dev.name;
    let jobTitle = dev.jobTitle;
    let company = dev.company;
    let experience = dev.experience;
    let school = dev.school;
    let major = dev.major;
    let email = dev.email;
    let linkedInUrl = dev.linkedInUrl;
    let codeLanguages = dev.codeLanguages;
}